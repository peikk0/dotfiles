[user]
    email = pierre@guinoiseau.nz
    name = Pierre Guinoiseau
    signingkey = 0x8D1C3224FAB5BA48

[advice]
    statusHints = false

[alias]
    br = branch
    ci = commit
    co = checkout
    cp = cherry-pick
    dc = diff --cached
    df = diff
    lg = log -p
    lgs = log --stat
    lol = log --graph --pretty=format:'%C(green)%h%C(magenta)%d%C(reset) %s %C(cyan)(%cr) %C(blue)-- %C(bold)%an%C(no-bold) <%ae>%C(reset)'
    lola = lol --all
    ls = ls-files
    st = status

[color]
    ui = auto
[color "branch"]
    current = green
    local = yellow
    remote = blue
[color "diff"]
    meta = blue
    frag = magenta
    old = red reverse
    new = green reverse
[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[commit]
    status = false

[core]
    excludesfile = ~/.config/git/ignore
    whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol

[delta]
    line-numbers = true
    syntax-theme = Nord

[diff]
    colorMoved = default
    renameLimit = 8192
    submodule = log

[filter "lfs"]
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process

[filter "media"]
    clean = git-media-clean %f
    smudge = git-media-smudge %f

[github]
    user = peikk0

[init]
    defaultBranch = main

[log]
    date = iso-local
    decorate = auto

[merge]
    conflictstyle = diff3

[pager]
    blame = delta
    diff = delta
    log = delta
    reflog = delta
    show = delta

[pull]
    rebase = false

[push]
    default = simple

[status]
    short = true

[include]
    path = ~/.config/git/config.local
